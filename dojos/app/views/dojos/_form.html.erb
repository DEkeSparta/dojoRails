<%= form_with(model: dojo, local: true) do |f| %>
  <% if dojo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dojo.errors.count, "error") %> prohibited this dojo from being saved:</h2>

      <ul>
      <% dojo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, autocomplete: "name" %>
  </div>

  <div class="field">
    <%= f.label :speciality %><br />
    <%= f.text_field :speciality, autocomplete: "speciality" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
